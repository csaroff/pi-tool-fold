{
  "name": "pi-tool-fold",
  "version": "0.1.0",
  "description": "Three inline transcript views for Pi: folded, regular, and fully expanded.",
  "type": "module",
  "license": "MIT",
  "author": "Chaskin Saroff",
  "repository": { "type": "git", "url": "https://github.com/csaroff/pi-tool-fold.git" },
  "keywords": ["pi-package", "pi-extension", "tools", "folding"],
  "files": ["index.ts", "src", "README.md", "LICENSE"],
  "pi": { "extensions": ["./index.ts"] },
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
